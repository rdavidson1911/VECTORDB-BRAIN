{
  "name": "omnikb-repo",
  "private": true,
  "description": "Root npm scripts delegate to web/ (package.json lives in web/).",
  "scripts": {
    "smoke:playwright": "npm --prefix web run smoke:playwright",
    "web:install": "npm --prefix web install",
    "web:dev": "npm --prefix web run dev",
    "web:build": "npm --prefix web run build",
    "web:lint": "npm --prefix web run lint"
  }
}
